<div class="p-5 mb-4 bg-light rounded-3">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold">Welcome!</h1>
    <p class="col-md-8 fs-4">You have successfully logged in.</p>
    <p>Your role is: <strong><%= current_user.role.humanize %></strong></p>
  </div>
</div>

<h4>Navigation Links (for testing):</h4>
<ul>
  <li><%= link_to "My Reservations", "#", class: "btn btn-primary" %></li>

  <% if current_user.can_manage_facilities? %>
    <li><%= link_to "Manage Facilities", "#", class: "btn btn-secondary" %></li>
  <% end %>

  <% if current_user.can_manage_system? %>
    <li><%= link_to "Admin Dashboard", "#", class: "btn btn-danger" %></li>
  <% end %>
</ul>
